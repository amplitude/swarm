{
  "name": "swarm",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "start:fake": "SWARM_FAKE=true node server.mjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:amplitude": "vitest run __amplitude_verify__.test.ts"
  },
  "dependencies": {
    "@amplitude/ai": "^0.15.0",
    "node-llama-cpp": "^3.1.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.62.1",
    "vitest": "^4.1.10"
  }
}
